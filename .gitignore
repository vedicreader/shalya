__pycache__/
*.py[cod]
.venv/
_docs/
_proc/
.ipynb_checkpoints/
dist/
build/
*.egg-info/
.pytest_cache/

# `save_media(session="s1")` writes beside the cwd, so a test run leaves images behind and
# `save_media` numbers the next one from what it finds
s1/
